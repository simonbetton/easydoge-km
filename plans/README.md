# Implementation Plans

Generated by the `improve` skill (deep audit) on 2026-09-04 against commit
`32f1e4d` (`main`, equal to `origin/main` at planning time). Audit evidence was
first collected at `04e7499` and every planned finding was re-read and
revalidated at `32f1e4d` before its plan was written.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, run every
verification command, and update your row when done.

**Selection note**: no operator was available to choose findings, so the top
five by leverage were planned, matching the audit handoff's recommended first
batch (findings 1–5). Findings 6–22 are listed under "Deferred findings" with
their original numbers so a later run can plan them without re-auditing.

**Repo gotcha for every executor**: `scripts/check-open-source-ready.sh`
(run by `./scripts/verify.sh`) fails if the uppercase marker words for
"to do" or "fix me" appear anywhere in the repository, including under
`plans/`. Never write them in code, docs, or these plan files. Status values
below deliberately avoid that word.

**Gradle gotcha for Kotlin plans**: the Android Gradle plugin's `test` task is
a lifecycle task and rejects `--tests`; run a single class with
`./gradlew :easydoge-km:testDebugUnitTest --tests '<fully.qualified.Class>'`.
Gradle 8.13 needs JDK 17: if the default `java` is newer, export `JAVA_HOME`
(and `ANDROID_HOME`) in the same shell command as `./gradlew` or
`./scripts/verify.sh`. After a passing run Gradle reports the test tasks
up-to-date; force re-execution with
`./gradlew cleanTestDebugUnitTest cleanTestReleaseUnitTest test`.

## Execution order & status

| Plan | Title | Finding | Priority | Effort | Depends on | Status |
|------|-------|---------|----------|--------|------------|--------|
| 001 | Reject undefined sighash types and the SIGHASH_SINGLE output-index bug | #5 | P1 | S | — | DONE (2026-09-06, reviewed and approved; commit `5321c2e` on branch `fix/sighash-type-validation`, PR https://github.com/simonbetton/easydoge-km/pull/40 open) |
| 002 | Authenticate Signing Envelopes before signing, combining, or finalizing | #1 | P1 | M | 001 | DONE (2026-09-06, reviewed and approved; commit `6a55829` on branch `claude/improve-execute-002-001bdc` (also local branch `fix/authenticate-signing-envelopes`), PR https://github.com/simonbetton/easydoge-km/pull/41 open. Plan amended before dispatch so the validator keeps the builder's one-input-at-a-time signing loop working; see the plan's Status block) |
| 003 | NFKD-normalize BIP39 passphrases before seed derivation | #2 | P1 | S | — | DONE (2026-09-06, reviewed and approved; commit `ef54326` on branch `fix/bip39-passphrase-nfkd`, not pushed, in worktree `.claude/worktrees/agent-ae953f036a7d2dfdd`) |
| 004 | Make Expo monetary and integer transport lossless and crash-free | #3 | P1 | L | — | DONE (2026-09-07, reviewed and approved; commit `a429374` on branch `fix/expo-lossless-koinu` in worktree `.claude/worktrees/agent-a1c388586e715125a`, cut from `599ca98`, PR https://github.com/simonbetton/easydoge-km/pull/43 open. Full `verify.sh` passes; Expo modules are not compiled by CI, so no on-device run was done) |
| 005 | Persist Android stored-wallet records so handles survive process death | #4 | P1 | M | — | DONE (2026-09-06, reviewed and approved; commits `d72fe07` + `f5517eb` on branch `fix/android-durable-wallet-records` in worktree `.claude/worktrees/agent-a6a51c33524e3612f`, not pushed, no PR. JVM unit tests and full `verify.sh` pass; the manual Android device check was not run) |

Status values: PENDING | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **002 requires 001**: the envelope validator in 002 calls `validated_sighash_flag`, which 001 introduces, and 002 removes 001's per-input check from `finalize_signing_envelope` when it moves that check into the validator. Land 001 first; run 002 only after 001 is DONE.
- **001/002 and 003 all append tests to `crates/easydoge-km/tests/parity.rs` and add entries to `CHANGELOG.md` / `docs/SECURITY_MODEL.md`.** They are logically independent; if executed in parallel, expect trivial merge conflicts in those three files only.
- **004 and 005 both touch `bindings/expo/android/.../EasyDogeKMModule.kt` and add files under `bindings/kotlin/easydoge-km/src`.** 004 rewrites the numeric converters; 005 changes one line (the store construction). Both are now DONE on separate branches, each cut from `599ca98`: 005 on `fix/android-durable-wallet-records` (the Expo module's `store` property became a `by lazy` block at lines 36–40) and 004 on `fix/expo-lossless-koinu` (import block and every converter). Their Expo-module hunks do not overlap; merging both also needs a trivial `CHANGELOG.md` resolution because each adds one bullet under `### Changed`.
- **003 needs its compatibility decision honored**: the plan documents the advisor's decision (spec compliance, no legacy mode, migration note in the changelog). If the operator disagrees, that is a STOP condition in the plan, not something the executor decides.
- Finding 6 (Expo enum parsing fails open) is deliberately kept out of 004 but should be the next Expo plan; 004 leaves room for a strict enum parser beside `WireCodec`.

## Verification baseline

`./scripts/verify.sh` passed in full on `04e7499` (format, 25 Rust tests,
Clippy with warnings denied, docs, bitcoinjs cross-check, generated bindings,
Swift and Kotlin wrapper tests, Expo typecheck). It has not been rerun on
`32f1e4d`; each plan's first verification step will surface any regression in
the intervening TUI/dependency/UniFFI-0.32 commits. Native toolchains needed:
Rust 1.91+, Swift 6, JDK 17 + Android SDK, Node 20 + pnpm.

`./scripts/verify.sh` passed in full on `6a55829` (plan 002 branch) on
2026-09-06. Local gotcha for executors on this machine: `bindings/kotlin`
needs `JAVA_HOME` pointing at a JDK 17 and `ANDROID_HOME` set (Gradle 8.13
does not run on the default JDK 24). Export both in the same shell command as
`./scripts/verify.sh`.

It also passed in full on `f5517eb` (plan 005 branch, 2026-09-06) and on
`a429374` (plan 004 branch, reviewer run on 2026-09-07).

Not covered by CI and therefore not machine-verifiable by any plan: the Expo
native module files (`bindings/expo/ios/*.swift`,
`bindings/expo/android/**/*.kt`) are never compiled (finding 15), and Android
Keystore behavior cannot run on the JVM. Plans 004 and 005 say so explicitly
and fall back to codec/repository unit tests plus grep-based checks and an
optional manual device check.

## Deferred findings (audited, vetted, not yet planned)

Numbers are the audit's; keep them when planning later. Evidence line numbers
were valid at `04e7499` and must be re-verified.

| # | Finding | Impact | Effort | Notes |
|---|---------|--------|--------|-------|
| 6 | Expo enum parsing fails open: unknown `network` / `language` / `protection` strings become mainnet / English / no-prompt (Expo iOS lines 168–197, Android 165–188) | high | S | Plan next; pair with 004's `WireCodec` pattern |
| 7 | Duplicate outpoints are counted and emitted by the builder (`transaction_builder.rs` 330–351, 464–467, 597–615) | high | S | Inflates funds; produces consensus-invalid tx |
| 8 | Android protected export is incomplete: no prompt-bound cipher flow, biometric and device-credential share one policy, requested export protection ignored (`AndroidKeystoreWalletSecretStore.kt`) | high | M | Do after 005 |
| 9 | CLI secrets travel through argv (`crates/easydoge-km-cli/src/main.rs` 81–189, `docs/CLI.md` 61–68) | high | M | Mnemonics/passphrases/xprivs/WIFs reach shell history and process lists |
| 10 | Secret DTOs leak through `Debug` formatting (`keys.rs:34` `GeneratedMnemonic`, FFI records, generated Kotlin) | high | M | Redact at every boundary |
| 11 | Uncompressed WIF state is discarded (`keys.rs` 286–295, 433–455; `message.rs` 20–67) | high | M | Fix risk HIGH: changes addresses for uncompressed WIFs |
| 12 | Multisig descriptors allow duplicate keys (`multisig.rs` 22–62; `signing.rs` 255–297) | high | S | Advertised signer counts may not be distinct signers |
| 13 | P2SH fee estimation trusts metadata instead of the redeem script (`transaction_builder.rs` 551–587, 775–794) | high | M | Underfunds and fails only at finalization |
| 14 | Expo package is not self-contained/installable (`bindings/expo/package.json`, Android `build.gradle`, podspec) | high | L | `npm pack --dry-run` lacks JS build and native deps |
| 15 | Expo native modules absent from the CI build graph (`scripts/verify.sh`) | high | L | Prerequisite for trusting 004/006 changes |
| 16 | Generated/published source identity not enforced (`scripts/generate-bindings.sh` 62–75, `verify.sh`, `package-release.sh` `--allow-dirty`) | high | S | Add a drift check; drop `--allow-dirty` |
| 17 | No resource caps at public request/envelope boundaries (CLI `main.rs:1051`, `signing.rs`, Expo `index.ts:264`) | medium | M | Do before 19; 002 adds O(n) ECDSA verification |
| 18 | `MinInputs` is not minimum-input selection (`transaction_builder.rs` 420–450, 524–547) | medium | M | Shares ordering with `LargestFirst`, prefix-only |
| 19 | Quadratic paths in large transaction work (`transaction_builder.rs` 420–472; signing) | medium | M | After 17 |
| 20 | Secret zeroization not implemented (`zeroize` declared, unused; secrets cloned) | medium | L | Needs FFI ownership design first |
| 21 | Native build inputs not reproducibly pinned (Gradle wrapper without checksum; Expo Modules Core `+`) | medium | S | Independent quick release gate |
| 22 | Parity harness has a patched moderate advisory (`valibot@1.4.1`, GHSA-5qjj-4xww-7phc) | low | S | Bump to `>=1.4.2` in `tools/bitcoinjs-cross-check` |

Direction candidates recorded by the audit (options, not defects): handle-only
derivation/signing plus wallet deletion and rotation so Expo never exports a
mnemonic into JavaScript; bounded batch xpub derivation for watch-only address
pools and gap scanning; multisig descriptor/envelope inspection with signer
fingerprints and completion status; a versioned schema-driven conformance layer
for the TypeScript, Swift, and Kotlin mappings.

## Findings considered and rejected

- TUI terminal-restoration error: present at `04e7499`, already fixed on `main` by `d9448ef`/`2f26108`. Not planned.
- Dogecoin network prefixes: cross-checked against Dogecoin Core and correct. Not a finding.
- Offline operation, Dogecoin-native extended-key defaults, native-only Expo (no Expo Go), and the independent bitcoinjs harness: accepted architectural decisions recorded in `docs/adr/0001`–`0005`. Not findings.
- Duplicate SHA-2 dependency versions and CI parallelization: no material impact demonstrated. Not promoted.
- No production credentials or prompt-injection artifacts were found in the repository.
