// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(easydoge_km_ffiFFI)
import easydoge_km_ffiFFI
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_easydoge_km_ffi_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_easydoge_km_ffi_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureEasydogeKmFfiInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
// Initial value and increment amount for handles.
// These ensure that SWIFT handles always have the lowest bit set
fileprivate let UNIFFI_HANDLEMAP_INITIAL: UInt64 = 1
fileprivate let UNIFFI_HANDLEMAP_DELTA: UInt64 = 2

fileprivate final class UniffiHandleMap<T>: @unchecked Sendable {
    // All mutation happens with this lock held, which is why we implement @unchecked Sendable.
    private let lock = NSLock()
    private var map: [UInt64: T] = [:]
    private var currentHandle: UInt64 = UNIFFI_HANDLEMAP_INITIAL

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            return doInsert(obj)
        }
    }

    // Low-level insert function, this assumes `lock` is held.
    private func doInsert(_ obj: T) -> UInt64 {
        let handle = currentHandle
        currentHandle += UNIFFI_HANDLEMAP_DELTA
        map[handle] = obj
        return handle
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

     func clone(handle: UInt64) throws -> UInt64 {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return doInsert(obj)
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt8: FfiConverterPrimitive {
    typealias FfiType = UInt8
    typealias SwiftType = UInt8

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt8 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: UInt8, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt16: FfiConverterPrimitive {
    typealias FfiType = UInt16
    typealias SwiftType = UInt16

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt16 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt32: FfiConverterPrimitive {
    typealias FfiType = UInt32
    typealias SwiftType = UInt32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt32: FfiConverterPrimitive {
    typealias FfiType = Int32
    typealias SwiftType = Int32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int32, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterBool : FfiConverter {
    typealias FfiType = Int8
    typealias SwiftType = Bool

    public static func lift(_ value: Int8) throws -> Bool {
        return value != 0
    }

    public static func lower(_ value: Bool) -> Int8 {
        return value ? 1 : 0
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bool {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Bool, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        // Use Swift's native UTF-8 decoder; `String(bytes:encoding:.utf8)` goes
        // through Foundation's NSString and silently strips a leading U+FEFF BOM.
        // Invalid UTF-8 substitutes U+FFFD instead of trapping (unreachable
        // given Rust's `String` invariant).
        return String(decoding: bytes, as: UTF8.self)
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        // See `lift` above for why we avoid Foundation's NSString-backed decoder here.
        return String(decoding: try readBytes(&buf, count: Int(len)), as: UTF8.self)
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}


public struct AccountKeySet: Equatable, Hashable {
    public var network: Network
    public var account: UInt32
    public var accountPath: String
    public var xpriv: Xpriv
    public var xpub: Xpub

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, account: UInt32, accountPath: String, xpriv: Xpriv, xpub: Xpub) {
        self.network = network
        self.account = account
        self.accountPath = accountPath
        self.xpriv = xpriv
        self.xpub = xpub
    }




}

#if compiler(>=6)
extension AccountKeySet: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAccountKeySet: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AccountKeySet {
        return
            try AccountKeySet(
                network: FfiConverterTypeNetwork.read(from: &buf),
                account: FfiConverterUInt32.read(from: &buf),
                accountPath: FfiConverterString.read(from: &buf),
                xpriv: FfiConverterTypeXpriv.read(from: &buf),
                xpub: FfiConverterTypeXpub.read(from: &buf)
        )
    }

    public static func write(_ value: AccountKeySet, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterUInt32.write(value.account, into: &buf)
        FfiConverterString.write(value.accountPath, into: &buf)
        FfiConverterTypeXpriv.write(value.xpriv, into: &buf)
        FfiConverterTypeXpub.write(value.xpub, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAccountKeySet_lift(_ buf: RustBuffer) throws -> AccountKeySet {
    return try FfiConverterTypeAccountKeySet.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAccountKeySet_lower(_ value: AccountKeySet) -> RustBuffer {
    return FfiConverterTypeAccountKeySet.lower(value)
}


public struct AuditedInput: Equatable, Hashable {
    public var txid: String
    public var vout: UInt32
    public var previousOutputValueKoinu: UInt64
    public var scriptPubkeyHex: String
    public var kind: SigningInputKind

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(txid: String, vout: UInt32, previousOutputValueKoinu: UInt64, scriptPubkeyHex: String, kind: SigningInputKind) {
        self.txid = txid
        self.vout = vout
        self.previousOutputValueKoinu = previousOutputValueKoinu
        self.scriptPubkeyHex = scriptPubkeyHex
        self.kind = kind
    }




}

#if compiler(>=6)
extension AuditedInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAuditedInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AuditedInput {
        return
            try AuditedInput(
                txid: FfiConverterString.read(from: &buf),
                vout: FfiConverterUInt32.read(from: &buf),
                previousOutputValueKoinu: FfiConverterUInt64.read(from: &buf),
                scriptPubkeyHex: FfiConverterString.read(from: &buf),
                kind: FfiConverterTypeSigningInputKind.read(from: &buf)
        )
    }

    public static func write(_ value: AuditedInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.txid, into: &buf)
        FfiConverterUInt32.write(value.vout, into: &buf)
        FfiConverterUInt64.write(value.previousOutputValueKoinu, into: &buf)
        FfiConverterString.write(value.scriptPubkeyHex, into: &buf)
        FfiConverterTypeSigningInputKind.write(value.kind, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuditedInput_lift(_ buf: RustBuffer) throws -> AuditedInput {
    return try FfiConverterTypeAuditedInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuditedInput_lower(_ value: AuditedInput) -> RustBuffer {
    return FfiConverterTypeAuditedInput.lower(value)
}


public struct ChangeDestination: Equatable, Hashable {
    public var address: String?
    public var xpriv: Xpriv?
    public var derivationPath: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(address: String?, xpriv: Xpriv?, derivationPath: String?) {
        self.address = address
        self.xpriv = xpriv
        self.derivationPath = derivationPath
    }




}

#if compiler(>=6)
extension ChangeDestination: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangeDestination: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangeDestination {
        return
            try ChangeDestination(
                address: FfiConverterOptionString.read(from: &buf),
                xpriv: FfiConverterOptionTypeXpriv.read(from: &buf),
                derivationPath: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: ChangeDestination, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.address, into: &buf)
        FfiConverterOptionTypeXpriv.write(value.xpriv, into: &buf)
        FfiConverterOptionString.write(value.derivationPath, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeDestination_lift(_ buf: RustBuffer) throws -> ChangeDestination {
    return try FfiConverterTypeChangeDestination.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeDestination_lower(_ value: ChangeDestination) -> RustBuffer {
    return FfiConverterTypeChangeDestination.lower(value)
}


public struct ComposeTransactionRequest: Equatable, Hashable {
    public var network: Network
    public var utxos: [SpendableUtxo]
    public var outputs: [TransactionOutput]
    public var feePolicy: FeePolicy
    public var coinSelection: CoinSelectionStrategy
    public var change: ChangeDestination?
    public var options: TransactionOptions

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, utxos: [SpendableUtxo], outputs: [TransactionOutput], feePolicy: FeePolicy, coinSelection: CoinSelectionStrategy, change: ChangeDestination?, options: TransactionOptions) {
        self.network = network
        self.utxos = utxos
        self.outputs = outputs
        self.feePolicy = feePolicy
        self.coinSelection = coinSelection
        self.change = change
        self.options = options
    }




}

#if compiler(>=6)
extension ComposeTransactionRequest: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeComposeTransactionRequest: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ComposeTransactionRequest {
        return
            try ComposeTransactionRequest(
                network: FfiConverterTypeNetwork.read(from: &buf),
                utxos: FfiConverterSequenceTypeSpendableUtxo.read(from: &buf),
                outputs: FfiConverterSequenceTypeTransactionOutput.read(from: &buf),
                feePolicy: FfiConverterTypeFeePolicy.read(from: &buf),
                coinSelection: FfiConverterTypeCoinSelectionStrategy.read(from: &buf),
                change: FfiConverterOptionTypeChangeDestination.read(from: &buf),
                options: FfiConverterTypeTransactionOptions.read(from: &buf)
        )
    }

    public static func write(_ value: ComposeTransactionRequest, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterSequenceTypeSpendableUtxo.write(value.utxos, into: &buf)
        FfiConverterSequenceTypeTransactionOutput.write(value.outputs, into: &buf)
        FfiConverterTypeFeePolicy.write(value.feePolicy, into: &buf)
        FfiConverterTypeCoinSelectionStrategy.write(value.coinSelection, into: &buf)
        FfiConverterOptionTypeChangeDestination.write(value.change, into: &buf)
        FfiConverterTypeTransactionOptions.write(value.options, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeComposeTransactionRequest_lift(_ buf: RustBuffer) throws -> ComposeTransactionRequest {
    return try FfiConverterTypeComposeTransactionRequest.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeComposeTransactionRequest_lower(_ value: ComposeTransactionRequest) -> RustBuffer {
    return FfiConverterTypeComposeTransactionRequest.lower(value)
}


public struct ComposeTransactionResult: Equatable, Hashable {
    public var network: Network
    public var selectedInputs: [AuditedInput]
    public var skippedInputs: [SkippedInput]
    public var inputTotalKoinu: UInt64
    public var spendOutputTotalKoinu: UInt64
    public var changeAmountKoinu: UInt64
    public var changeAddress: String?
    public var changeScriptPubkeyHex: String?
    public var feeKoinu: UInt64
    public var estimatedSizeBytes: UInt64
    public var actualSizeBytes: UInt64?
    public var dustChangeFoldedIntoFee: Bool
    public var unsignedTxHex: String
    public var signedTxHex: String?
    public var signingEnvelope: SigningEnvelope?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, selectedInputs: [AuditedInput], skippedInputs: [SkippedInput], inputTotalKoinu: UInt64, spendOutputTotalKoinu: UInt64, changeAmountKoinu: UInt64, changeAddress: String?, changeScriptPubkeyHex: String?, feeKoinu: UInt64, estimatedSizeBytes: UInt64, actualSizeBytes: UInt64?, dustChangeFoldedIntoFee: Bool, unsignedTxHex: String, signedTxHex: String?, signingEnvelope: SigningEnvelope?) {
        self.network = network
        self.selectedInputs = selectedInputs
        self.skippedInputs = skippedInputs
        self.inputTotalKoinu = inputTotalKoinu
        self.spendOutputTotalKoinu = spendOutputTotalKoinu
        self.changeAmountKoinu = changeAmountKoinu
        self.changeAddress = changeAddress
        self.changeScriptPubkeyHex = changeScriptPubkeyHex
        self.feeKoinu = feeKoinu
        self.estimatedSizeBytes = estimatedSizeBytes
        self.actualSizeBytes = actualSizeBytes
        self.dustChangeFoldedIntoFee = dustChangeFoldedIntoFee
        self.unsignedTxHex = unsignedTxHex
        self.signedTxHex = signedTxHex
        self.signingEnvelope = signingEnvelope
    }




}

#if compiler(>=6)
extension ComposeTransactionResult: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeComposeTransactionResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ComposeTransactionResult {
        return
            try ComposeTransactionResult(
                network: FfiConverterTypeNetwork.read(from: &buf),
                selectedInputs: FfiConverterSequenceTypeAuditedInput.read(from: &buf),
                skippedInputs: FfiConverterSequenceTypeSkippedInput.read(from: &buf),
                inputTotalKoinu: FfiConverterUInt64.read(from: &buf),
                spendOutputTotalKoinu: FfiConverterUInt64.read(from: &buf),
                changeAmountKoinu: FfiConverterUInt64.read(from: &buf),
                changeAddress: FfiConverterOptionString.read(from: &buf),
                changeScriptPubkeyHex: FfiConverterOptionString.read(from: &buf),
                feeKoinu: FfiConverterUInt64.read(from: &buf),
                estimatedSizeBytes: FfiConverterUInt64.read(from: &buf),
                actualSizeBytes: FfiConverterOptionUInt64.read(from: &buf),
                dustChangeFoldedIntoFee: FfiConverterBool.read(from: &buf),
                unsignedTxHex: FfiConverterString.read(from: &buf),
                signedTxHex: FfiConverterOptionString.read(from: &buf),
                signingEnvelope: FfiConverterOptionTypeSigningEnvelope.read(from: &buf)
        )
    }

    public static func write(_ value: ComposeTransactionResult, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterSequenceTypeAuditedInput.write(value.selectedInputs, into: &buf)
        FfiConverterSequenceTypeSkippedInput.write(value.skippedInputs, into: &buf)
        FfiConverterUInt64.write(value.inputTotalKoinu, into: &buf)
        FfiConverterUInt64.write(value.spendOutputTotalKoinu, into: &buf)
        FfiConverterUInt64.write(value.changeAmountKoinu, into: &buf)
        FfiConverterOptionString.write(value.changeAddress, into: &buf)
        FfiConverterOptionString.write(value.changeScriptPubkeyHex, into: &buf)
        FfiConverterUInt64.write(value.feeKoinu, into: &buf)
        FfiConverterUInt64.write(value.estimatedSizeBytes, into: &buf)
        FfiConverterOptionUInt64.write(value.actualSizeBytes, into: &buf)
        FfiConverterBool.write(value.dustChangeFoldedIntoFee, into: &buf)
        FfiConverterString.write(value.unsignedTxHex, into: &buf)
        FfiConverterOptionString.write(value.signedTxHex, into: &buf)
        FfiConverterOptionTypeSigningEnvelope.write(value.signingEnvelope, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeComposeTransactionResult_lift(_ buf: RustBuffer) throws -> ComposeTransactionResult {
    return try FfiConverterTypeComposeTransactionResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeComposeTransactionResult_lower(_ value: ComposeTransactionResult) -> RustBuffer {
    return FfiConverterTypeComposeTransactionResult.lower(value)
}


public struct ExtendedKeyInfo: Equatable, Hashable {
    public var network: Network
    public var depth: UInt8
    public var parentFingerprintHex: String
    public var childNumber: UInt32
    public var publicKeyHex: String?
    public var privateKeyRedacted: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, depth: UInt8, parentFingerprintHex: String, childNumber: UInt32, publicKeyHex: String?, privateKeyRedacted: Bool) {
        self.network = network
        self.depth = depth
        self.parentFingerprintHex = parentFingerprintHex
        self.childNumber = childNumber
        self.publicKeyHex = publicKeyHex
        self.privateKeyRedacted = privateKeyRedacted
    }




}

#if compiler(>=6)
extension ExtendedKeyInfo: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExtendedKeyInfo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExtendedKeyInfo {
        return
            try ExtendedKeyInfo(
                network: FfiConverterTypeNetwork.read(from: &buf),
                depth: FfiConverterUInt8.read(from: &buf),
                parentFingerprintHex: FfiConverterString.read(from: &buf),
                childNumber: FfiConverterUInt32.read(from: &buf),
                publicKeyHex: FfiConverterOptionString.read(from: &buf),
                privateKeyRedacted: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: ExtendedKeyInfo, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterUInt8.write(value.depth, into: &buf)
        FfiConverterString.write(value.parentFingerprintHex, into: &buf)
        FfiConverterUInt32.write(value.childNumber, into: &buf)
        FfiConverterOptionString.write(value.publicKeyHex, into: &buf)
        FfiConverterBool.write(value.privateKeyRedacted, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExtendedKeyInfo_lift(_ buf: RustBuffer) throws -> ExtendedKeyInfo {
    return try FfiConverterTypeExtendedKeyInfo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExtendedKeyInfo_lower(_ value: ExtendedKeyInfo) -> RustBuffer {
    return FfiConverterTypeExtendedKeyInfo.lower(value)
}


public struct FeePolicy: Equatable, Hashable {
    public var feeRateKoinuPerKb: UInt64
    public var dustThresholdKoinu: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(feeRateKoinuPerKb: UInt64, dustThresholdKoinu: UInt64) {
        self.feeRateKoinuPerKb = feeRateKoinuPerKb
        self.dustThresholdKoinu = dustThresholdKoinu
    }




}

#if compiler(>=6)
extension FeePolicy: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeFeePolicy: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> FeePolicy {
        return
            try FeePolicy(
                feeRateKoinuPerKb: FfiConverterUInt64.read(from: &buf),
                dustThresholdKoinu: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: FeePolicy, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.feeRateKoinuPerKb, into: &buf)
        FfiConverterUInt64.write(value.dustThresholdKoinu, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFeePolicy_lift(_ buf: RustBuffer) throws -> FeePolicy {
    return try FfiConverterTypeFeePolicy.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFeePolicy_lower(_ value: FeePolicy) -> RustBuffer {
    return FfiConverterTypeFeePolicy.lower(value)
}


public struct GeneratedMnemonic: Equatable, Hashable {
    public var phrase: String
    public var language: Language
    public var wordCount: UInt16

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(phrase: String, language: Language, wordCount: UInt16) {
        self.phrase = phrase
        self.language = language
        self.wordCount = wordCount
    }




}

#if compiler(>=6)
extension GeneratedMnemonic: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGeneratedMnemonic: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GeneratedMnemonic {
        return
            try GeneratedMnemonic(
                phrase: FfiConverterString.read(from: &buf),
                language: FfiConverterTypeLanguage.read(from: &buf),
                wordCount: FfiConverterUInt16.read(from: &buf)
        )
    }

    public static func write(_ value: GeneratedMnemonic, into buf: inout [UInt8]) {
        FfiConverterString.write(value.phrase, into: &buf)
        FfiConverterTypeLanguage.write(value.language, into: &buf)
        FfiConverterUInt16.write(value.wordCount, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGeneratedMnemonic_lift(_ buf: RustBuffer) throws -> GeneratedMnemonic {
    return try FfiConverterTypeGeneratedMnemonic.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGeneratedMnemonic_lower(_ value: GeneratedMnemonic) -> RustBuffer {
    return FfiConverterTypeGeneratedMnemonic.lower(value)
}


public struct MessageSignature: Equatable, Hashable {
    public var network: Network
    public var address: String
    public var signatureBase64: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, address: String, signatureBase64: String) {
        self.network = network
        self.address = address
        self.signatureBase64 = signatureBase64
    }




}

#if compiler(>=6)
extension MessageSignature: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMessageSignature: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MessageSignature {
        return
            try MessageSignature(
                network: FfiConverterTypeNetwork.read(from: &buf),
                address: FfiConverterString.read(from: &buf),
                signatureBase64: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: MessageSignature, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.address, into: &buf)
        FfiConverterString.write(value.signatureBase64, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMessageSignature_lift(_ buf: RustBuffer) throws -> MessageSignature {
    return try FfiConverterTypeMessageSignature.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMessageSignature_lower(_ value: MessageSignature) -> RustBuffer {
    return FfiConverterTypeMessageSignature.lower(value)
}


public struct MnemonicOptions: Equatable, Hashable {
    public var language: Language
    public var wordCount: UInt16

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(language: Language, wordCount: UInt16) {
        self.language = language
        self.wordCount = wordCount
    }




}

#if compiler(>=6)
extension MnemonicOptions: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMnemonicOptions: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MnemonicOptions {
        return
            try MnemonicOptions(
                language: FfiConverterTypeLanguage.read(from: &buf),
                wordCount: FfiConverterUInt16.read(from: &buf)
        )
    }

    public static func write(_ value: MnemonicOptions, into buf: inout [UInt8]) {
        FfiConverterTypeLanguage.write(value.language, into: &buf)
        FfiConverterUInt16.write(value.wordCount, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMnemonicOptions_lift(_ buf: RustBuffer) throws -> MnemonicOptions {
    return try FfiConverterTypeMnemonicOptions.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMnemonicOptions_lower(_ value: MnemonicOptions) -> RustBuffer {
    return FfiConverterTypeMnemonicOptions.lower(value)
}


public struct MultisigDescriptor: Equatable, Hashable {
    public var network: Network
    public var threshold: UInt8
    public var cosignerCount: UInt8
    public var childPath: String
    public var sorted: Bool
    public var publicKeysHex: [String]
    public var redeemScriptHex: String
    public var p2shAddress: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, threshold: UInt8, cosignerCount: UInt8, childPath: String, sorted: Bool, publicKeysHex: [String], redeemScriptHex: String, p2shAddress: String) {
        self.network = network
        self.threshold = threshold
        self.cosignerCount = cosignerCount
        self.childPath = childPath
        self.sorted = sorted
        self.publicKeysHex = publicKeysHex
        self.redeemScriptHex = redeemScriptHex
        self.p2shAddress = p2shAddress
    }




}

#if compiler(>=6)
extension MultisigDescriptor: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigDescriptor: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigDescriptor {
        return
            try MultisigDescriptor(
                network: FfiConverterTypeNetwork.read(from: &buf),
                threshold: FfiConverterUInt8.read(from: &buf),
                cosignerCount: FfiConverterUInt8.read(from: &buf),
                childPath: FfiConverterString.read(from: &buf),
                sorted: FfiConverterBool.read(from: &buf),
                publicKeysHex: FfiConverterSequenceString.read(from: &buf),
                redeemScriptHex: FfiConverterString.read(from: &buf),
                p2shAddress: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: MultisigDescriptor, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterUInt8.write(value.threshold, into: &buf)
        FfiConverterUInt8.write(value.cosignerCount, into: &buf)
        FfiConverterString.write(value.childPath, into: &buf)
        FfiConverterBool.write(value.sorted, into: &buf)
        FfiConverterSequenceString.write(value.publicKeysHex, into: &buf)
        FfiConverterString.write(value.redeemScriptHex, into: &buf)
        FfiConverterString.write(value.p2shAddress, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigDescriptor_lift(_ buf: RustBuffer) throws -> MultisigDescriptor {
    return try FfiConverterTypeMultisigDescriptor.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigDescriptor_lower(_ value: MultisigDescriptor) -> RustBuffer {
    return FfiConverterTypeMultisigDescriptor.lower(value)
}


public struct PathAddress: Equatable, Hashable {
    public var network: Network
    public var path: String
    public var publicKeyHex: String
    public var address: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, path: String, publicKeyHex: String, address: String) {
        self.network = network
        self.path = path
        self.publicKeyHex = publicKeyHex
        self.address = address
    }




}

#if compiler(>=6)
extension PathAddress: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePathAddress: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PathAddress {
        return
            try PathAddress(
                network: FfiConverterTypeNetwork.read(from: &buf),
                path: FfiConverterString.read(from: &buf),
                publicKeyHex: FfiConverterString.read(from: &buf),
                address: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: PathAddress, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.path, into: &buf)
        FfiConverterString.write(value.publicKeyHex, into: &buf)
        FfiConverterString.write(value.address, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePathAddress_lift(_ buf: RustBuffer) throws -> PathAddress {
    return try FfiConverterTypePathAddress.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePathAddress_lower(_ value: PathAddress) -> RustBuffer {
    return FfiConverterTypePathAddress.lower(value)
}


public struct SignedTransaction: Equatable, Hashable {
    public var network: Network
    public var signedTxHex: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, signedTxHex: String) {
        self.network = network
        self.signedTxHex = signedTxHex
    }




}

#if compiler(>=6)
extension SignedTransaction: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSignedTransaction: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SignedTransaction {
        return
            try SignedTransaction(
                network: FfiConverterTypeNetwork.read(from: &buf),
                signedTxHex: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: SignedTransaction, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.signedTxHex, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransaction_lift(_ buf: RustBuffer) throws -> SignedTransaction {
    return try FfiConverterTypeSignedTransaction.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransaction_lower(_ value: SignedTransaction) -> RustBuffer {
    return FfiConverterTypeSignedTransaction.lower(value)
}


public struct SigningEnvelope: Equatable, Hashable {
    public var version: UInt8
    public var network: Network
    public var unsignedTxHex: String
    public var inputs: [SigningEnvelopeInput]
    public var signatures: [SigningEnvelopeSignature]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(version: UInt8, network: Network, unsignedTxHex: String, inputs: [SigningEnvelopeInput], signatures: [SigningEnvelopeSignature]) {
        self.version = version
        self.network = network
        self.unsignedTxHex = unsignedTxHex
        self.inputs = inputs
        self.signatures = signatures
    }




}

#if compiler(>=6)
extension SigningEnvelope: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSigningEnvelope: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SigningEnvelope {
        return
            try SigningEnvelope(
                version: FfiConverterUInt8.read(from: &buf),
                network: FfiConverterTypeNetwork.read(from: &buf),
                unsignedTxHex: FfiConverterString.read(from: &buf),
                inputs: FfiConverterSequenceTypeSigningEnvelopeInput.read(from: &buf),
                signatures: FfiConverterSequenceTypeSigningEnvelopeSignature.read(from: &buf)
        )
    }

    public static func write(_ value: SigningEnvelope, into buf: inout [UInt8]) {
        FfiConverterUInt8.write(value.version, into: &buf)
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.unsignedTxHex, into: &buf)
        FfiConverterSequenceTypeSigningEnvelopeInput.write(value.inputs, into: &buf)
        FfiConverterSequenceTypeSigningEnvelopeSignature.write(value.signatures, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelope_lift(_ buf: RustBuffer) throws -> SigningEnvelope {
    return try FfiConverterTypeSigningEnvelope.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelope_lower(_ value: SigningEnvelope) -> RustBuffer {
    return FfiConverterTypeSigningEnvelope.lower(value)
}


public struct SigningEnvelopeInput: Equatable, Hashable {
    public var inputIndex: UInt64
    public var kind: SigningInputKind
    public var scriptPubkeyHex: String
    public var redeemScriptHex: String?
    public var sighashType: UInt32
    public var previousOutputValueKoinu: UInt64?
    public var multisigThreshold: UInt8?
    public var multisigPublicKeysHex: [String]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(inputIndex: UInt64, kind: SigningInputKind, scriptPubkeyHex: String, redeemScriptHex: String?, sighashType: UInt32, previousOutputValueKoinu: UInt64?, multisigThreshold: UInt8?, multisigPublicKeysHex: [String]) {
        self.inputIndex = inputIndex
        self.kind = kind
        self.scriptPubkeyHex = scriptPubkeyHex
        self.redeemScriptHex = redeemScriptHex
        self.sighashType = sighashType
        self.previousOutputValueKoinu = previousOutputValueKoinu
        self.multisigThreshold = multisigThreshold
        self.multisigPublicKeysHex = multisigPublicKeysHex
    }




}

#if compiler(>=6)
extension SigningEnvelopeInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSigningEnvelopeInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SigningEnvelopeInput {
        return
            try SigningEnvelopeInput(
                inputIndex: FfiConverterUInt64.read(from: &buf),
                kind: FfiConverterTypeSigningInputKind.read(from: &buf),
                scriptPubkeyHex: FfiConverterString.read(from: &buf),
                redeemScriptHex: FfiConverterOptionString.read(from: &buf),
                sighashType: FfiConverterUInt32.read(from: &buf),
                previousOutputValueKoinu: FfiConverterOptionUInt64.read(from: &buf),
                multisigThreshold: FfiConverterOptionUInt8.read(from: &buf),
                multisigPublicKeysHex: FfiConverterSequenceString.read(from: &buf)
        )
    }

    public static func write(_ value: SigningEnvelopeInput, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.inputIndex, into: &buf)
        FfiConverterTypeSigningInputKind.write(value.kind, into: &buf)
        FfiConverterString.write(value.scriptPubkeyHex, into: &buf)
        FfiConverterOptionString.write(value.redeemScriptHex, into: &buf)
        FfiConverterUInt32.write(value.sighashType, into: &buf)
        FfiConverterOptionUInt64.write(value.previousOutputValueKoinu, into: &buf)
        FfiConverterOptionUInt8.write(value.multisigThreshold, into: &buf)
        FfiConverterSequenceString.write(value.multisigPublicKeysHex, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelopeInput_lift(_ buf: RustBuffer) throws -> SigningEnvelopeInput {
    return try FfiConverterTypeSigningEnvelopeInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelopeInput_lower(_ value: SigningEnvelopeInput) -> RustBuffer {
    return FfiConverterTypeSigningEnvelopeInput.lower(value)
}


public struct SigningEnvelopeSignature: Equatable, Hashable {
    public var inputIndex: UInt64
    public var publicKeyHex: String
    public var signatureHex: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(inputIndex: UInt64, publicKeyHex: String, signatureHex: String) {
        self.inputIndex = inputIndex
        self.publicKeyHex = publicKeyHex
        self.signatureHex = signatureHex
    }




}

#if compiler(>=6)
extension SigningEnvelopeSignature: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSigningEnvelopeSignature: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SigningEnvelopeSignature {
        return
            try SigningEnvelopeSignature(
                inputIndex: FfiConverterUInt64.read(from: &buf),
                publicKeyHex: FfiConverterString.read(from: &buf),
                signatureHex: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: SigningEnvelopeSignature, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.inputIndex, into: &buf)
        FfiConverterString.write(value.publicKeyHex, into: &buf)
        FfiConverterString.write(value.signatureHex, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelopeSignature_lift(_ buf: RustBuffer) throws -> SigningEnvelopeSignature {
    return try FfiConverterTypeSigningEnvelopeSignature.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningEnvelopeSignature_lower(_ value: SigningEnvelopeSignature) -> RustBuffer {
    return FfiConverterTypeSigningEnvelopeSignature.lower(value)
}


public struct SkippedInput: Equatable, Hashable {
    public var txid: String
    public var vout: UInt32
    public var previousOutputValueKoinu: UInt64
    public var reason: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(txid: String, vout: UInt32, previousOutputValueKoinu: UInt64, reason: String) {
        self.txid = txid
        self.vout = vout
        self.previousOutputValueKoinu = previousOutputValueKoinu
        self.reason = reason
    }




}

#if compiler(>=6)
extension SkippedInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSkippedInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SkippedInput {
        return
            try SkippedInput(
                txid: FfiConverterString.read(from: &buf),
                vout: FfiConverterUInt32.read(from: &buf),
                previousOutputValueKoinu: FfiConverterUInt64.read(from: &buf),
                reason: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: SkippedInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.txid, into: &buf)
        FfiConverterUInt32.write(value.vout, into: &buf)
        FfiConverterUInt64.write(value.previousOutputValueKoinu, into: &buf)
        FfiConverterString.write(value.reason, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSkippedInput_lift(_ buf: RustBuffer) throws -> SkippedInput {
    return try FfiConverterTypeSkippedInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSkippedInput_lower(_ value: SkippedInput) -> RustBuffer {
    return FfiConverterTypeSkippedInput.lower(value)
}


public struct SpendableUtxo: Equatable, Hashable {
    public var txid: String
    public var vout: UInt32
    public var previousOutputValueKoinu: UInt64
    public var scriptPubkeyHex: String
    public var kind: SigningInputKind
    public var redeemScriptHex: String?
    public var multisigThreshold: UInt8?
    public var multisigPublicKeysHex: [String]
    public var signers: [UtxoSigner]
    public var manuallySelected: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(txid: String, vout: UInt32, previousOutputValueKoinu: UInt64, scriptPubkeyHex: String, kind: SigningInputKind, redeemScriptHex: String?, multisigThreshold: UInt8?, multisigPublicKeysHex: [String], signers: [UtxoSigner], manuallySelected: Bool) {
        self.txid = txid
        self.vout = vout
        self.previousOutputValueKoinu = previousOutputValueKoinu
        self.scriptPubkeyHex = scriptPubkeyHex
        self.kind = kind
        self.redeemScriptHex = redeemScriptHex
        self.multisigThreshold = multisigThreshold
        self.multisigPublicKeysHex = multisigPublicKeysHex
        self.signers = signers
        self.manuallySelected = manuallySelected
    }




}

#if compiler(>=6)
extension SpendableUtxo: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSpendableUtxo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SpendableUtxo {
        return
            try SpendableUtxo(
                txid: FfiConverterString.read(from: &buf),
                vout: FfiConverterUInt32.read(from: &buf),
                previousOutputValueKoinu: FfiConverterUInt64.read(from: &buf),
                scriptPubkeyHex: FfiConverterString.read(from: &buf),
                kind: FfiConverterTypeSigningInputKind.read(from: &buf),
                redeemScriptHex: FfiConverterOptionString.read(from: &buf),
                multisigThreshold: FfiConverterOptionUInt8.read(from: &buf),
                multisigPublicKeysHex: FfiConverterSequenceString.read(from: &buf),
                signers: FfiConverterSequenceTypeUtxoSigner.read(from: &buf),
                manuallySelected: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: SpendableUtxo, into buf: inout [UInt8]) {
        FfiConverterString.write(value.txid, into: &buf)
        FfiConverterUInt32.write(value.vout, into: &buf)
        FfiConverterUInt64.write(value.previousOutputValueKoinu, into: &buf)
        FfiConverterString.write(value.scriptPubkeyHex, into: &buf)
        FfiConverterTypeSigningInputKind.write(value.kind, into: &buf)
        FfiConverterOptionString.write(value.redeemScriptHex, into: &buf)
        FfiConverterOptionUInt8.write(value.multisigThreshold, into: &buf)
        FfiConverterSequenceString.write(value.multisigPublicKeysHex, into: &buf)
        FfiConverterSequenceTypeUtxoSigner.write(value.signers, into: &buf)
        FfiConverterBool.write(value.manuallySelected, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpendableUtxo_lift(_ buf: RustBuffer) throws -> SpendableUtxo {
    return try FfiConverterTypeSpendableUtxo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpendableUtxo_lower(_ value: SpendableUtxo) -> RustBuffer {
    return FfiConverterTypeSpendableUtxo.lower(value)
}


public struct TransactionOptions: Equatable, Hashable {
    public var version: Int32
    public var lockTime: UInt32
    public var sequence: UInt32
    public var sighashType: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(version: Int32, lockTime: UInt32, sequence: UInt32, sighashType: UInt32) {
        self.version = version
        self.lockTime = lockTime
        self.sequence = sequence
        self.sighashType = sighashType
    }




}

#if compiler(>=6)
extension TransactionOptions: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionOptions: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionOptions {
        return
            try TransactionOptions(
                version: FfiConverterInt32.read(from: &buf),
                lockTime: FfiConverterUInt32.read(from: &buf),
                sequence: FfiConverterUInt32.read(from: &buf),
                sighashType: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionOptions, into buf: inout [UInt8]) {
        FfiConverterInt32.write(value.version, into: &buf)
        FfiConverterUInt32.write(value.lockTime, into: &buf)
        FfiConverterUInt32.write(value.sequence, into: &buf)
        FfiConverterUInt32.write(value.sighashType, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOptions_lift(_ buf: RustBuffer) throws -> TransactionOptions {
    return try FfiConverterTypeTransactionOptions.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOptions_lower(_ value: TransactionOptions) -> RustBuffer {
    return FfiConverterTypeTransactionOptions.lower(value)
}


public struct TransactionOutput: Equatable, Hashable {
    public var kind: TransactionOutputKind
    public var valueKoinu: UInt64
    public var address: String?
    public var opReturnDataHex: String?
    public var scriptHex: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(kind: TransactionOutputKind, valueKoinu: UInt64, address: String?, opReturnDataHex: String?, scriptHex: String?) {
        self.kind = kind
        self.valueKoinu = valueKoinu
        self.address = address
        self.opReturnDataHex = opReturnDataHex
        self.scriptHex = scriptHex
    }




}

#if compiler(>=6)
extension TransactionOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionOutput {
        return
            try TransactionOutput(
                kind: FfiConverterTypeTransactionOutputKind.read(from: &buf),
                valueKoinu: FfiConverterUInt64.read(from: &buf),
                address: FfiConverterOptionString.read(from: &buf),
                opReturnDataHex: FfiConverterOptionString.read(from: &buf),
                scriptHex: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionOutput, into buf: inout [UInt8]) {
        FfiConverterTypeTransactionOutputKind.write(value.kind, into: &buf)
        FfiConverterUInt64.write(value.valueKoinu, into: &buf)
        FfiConverterOptionString.write(value.address, into: &buf)
        FfiConverterOptionString.write(value.opReturnDataHex, into: &buf)
        FfiConverterOptionString.write(value.scriptHex, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOutput_lift(_ buf: RustBuffer) throws -> TransactionOutput {
    return try FfiConverterTypeTransactionOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOutput_lower(_ value: TransactionOutput) -> RustBuffer {
    return FfiConverterTypeTransactionOutput.lower(value)
}


public struct UtxoSigner: Equatable, Hashable {
    public var kind: UtxoSignerKind
    public var wif: String?
    public var xpriv: Xpriv?
    public var derivationPath: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(kind: UtxoSignerKind, wif: String?, xpriv: Xpriv?, derivationPath: String?) {
        self.kind = kind
        self.wif = wif
        self.xpriv = xpriv
        self.derivationPath = derivationPath
    }




}

#if compiler(>=6)
extension UtxoSigner: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUtxoSigner: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UtxoSigner {
        return
            try UtxoSigner(
                kind: FfiConverterTypeUtxoSignerKind.read(from: &buf),
                wif: FfiConverterOptionString.read(from: &buf),
                xpriv: FfiConverterOptionTypeXpriv.read(from: &buf),
                derivationPath: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: UtxoSigner, into buf: inout [UInt8]) {
        FfiConverterTypeUtxoSignerKind.write(value.kind, into: &buf)
        FfiConverterOptionString.write(value.wif, into: &buf)
        FfiConverterOptionTypeXpriv.write(value.xpriv, into: &buf)
        FfiConverterOptionString.write(value.derivationPath, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUtxoSigner_lift(_ buf: RustBuffer) throws -> UtxoSigner {
    return try FfiConverterTypeUtxoSigner.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUtxoSigner_lower(_ value: UtxoSigner) -> RustBuffer {
    return FfiConverterTypeUtxoSigner.lower(value)
}


public struct WifInfo: Equatable, Hashable {
    public var network: Network
    public var publicKeyHex: String
    public var address: String
    public var compressed: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, publicKeyHex: String, address: String, compressed: Bool) {
        self.network = network
        self.publicKeyHex = publicKeyHex
        self.address = address
        self.compressed = compressed
    }




}

#if compiler(>=6)
extension WifInfo: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWifInfo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WifInfo {
        return
            try WifInfo(
                network: FfiConverterTypeNetwork.read(from: &buf),
                publicKeyHex: FfiConverterString.read(from: &buf),
                address: FfiConverterString.read(from: &buf),
                compressed: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: WifInfo, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.publicKeyHex, into: &buf)
        FfiConverterString.write(value.address, into: &buf)
        FfiConverterBool.write(value.compressed, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWifInfo_lift(_ buf: RustBuffer) throws -> WifInfo {
    return try FfiConverterTypeWifInfo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWifInfo_lower(_ value: WifInfo) -> RustBuffer {
    return FfiConverterTypeWifInfo.lower(value)
}


public struct Xpriv: Equatable, Hashable {
    public var network: Network
    public var encoded: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, encoded: String) {
        self.network = network
        self.encoded = encoded
    }




}

#if compiler(>=6)
extension Xpriv: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeXpriv: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Xpriv {
        return
            try Xpriv(
                network: FfiConverterTypeNetwork.read(from: &buf),
                encoded: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: Xpriv, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.encoded, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeXpriv_lift(_ buf: RustBuffer) throws -> Xpriv {
    return try FfiConverterTypeXpriv.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeXpriv_lower(_ value: Xpriv) -> RustBuffer {
    return FfiConverterTypeXpriv.lower(value)
}


public struct Xpub: Equatable, Hashable {
    public var network: Network
    public var encoded: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(network: Network, encoded: String) {
        self.network = network
        self.encoded = encoded
    }




}

#if compiler(>=6)
extension Xpub: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeXpub: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Xpub {
        return
            try Xpub(
                network: FfiConverterTypeNetwork.read(from: &buf),
                encoded: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: Xpub, into buf: inout [UInt8]) {
        FfiConverterTypeNetwork.write(value.network, into: &buf)
        FfiConverterString.write(value.encoded, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeXpub_lift(_ buf: RustBuffer) throws -> Xpub {
    return try FfiConverterTypeXpub.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeXpub_lower(_ value: Xpub) -> RustBuffer {
    return FfiConverterTypeXpub.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum CoinSelectionStrategy: Equatable, Hashable {

    case minInputs
    case smallestFirst
    case largestFirst
    case manualSelectedInputs





}

#if compiler(>=6)
extension CoinSelectionStrategy: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCoinSelectionStrategy: FfiConverterRustBuffer {
    typealias SwiftType = CoinSelectionStrategy

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CoinSelectionStrategy {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .minInputs

        case 2: return .smallestFirst

        case 3: return .largestFirst

        case 4: return .manualSelectedInputs

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: CoinSelectionStrategy, into buf: inout [UInt8]) {
        switch value {


        case .minInputs:
            writeInt(&buf, Int32(1))


        case .smallestFirst:
            writeInt(&buf, Int32(2))


        case .largestFirst:
            writeInt(&buf, Int32(3))


        case .manualSelectedInputs:
            writeInt(&buf, Int32(4))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinSelectionStrategy_lift(_ buf: RustBuffer) throws -> CoinSelectionStrategy {
    return try FfiConverterTypeCoinSelectionStrategy.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinSelectionStrategy_lower(_ value: CoinSelectionStrategy) -> RustBuffer {
    return FfiConverterTypeCoinSelectionStrategy.lower(value)
}



public enum FfiError: Swift.Error, Equatable, Hashable, Foundation.LocalizedError {



    case Failure(reason: String
    )






    public var errorDescription: String? {
        String(reflecting: self)
    }

}

#if compiler(>=6)
extension FfiError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeFfiError: FfiConverterRustBuffer {
    typealias SwiftType = FfiError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> FfiError {
        let variant: Int32 = try readInt(&buf)
        switch variant {




        case 1: return .Failure(
            reason: try FfiConverterString.read(from: &buf)
            )

         default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: FfiError, into buf: inout [UInt8]) {
        switch value {





        case let .Failure(reason):
            writeInt(&buf, Int32(1))
            FfiConverterString.write(reason, into: &buf)

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFfiError_lift(_ buf: RustBuffer) throws -> FfiError {
    return try FfiConverterTypeFfiError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFfiError_lower(_ value: FfiError) -> RustBuffer {
    return FfiConverterTypeFfiError.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum Language: Equatable, Hashable {

    case english
    case simplifiedChinese
    case traditionalChinese
    case czech
    case french
    case italian
    case japanese
    case korean
    case portuguese
    case spanish





}

#if compiler(>=6)
extension Language: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeLanguage: FfiConverterRustBuffer {
    typealias SwiftType = Language

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Language {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .english

        case 2: return .simplifiedChinese

        case 3: return .traditionalChinese

        case 4: return .czech

        case 5: return .french

        case 6: return .italian

        case 7: return .japanese

        case 8: return .korean

        case 9: return .portuguese

        case 10: return .spanish

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: Language, into buf: inout [UInt8]) {
        switch value {


        case .english:
            writeInt(&buf, Int32(1))


        case .simplifiedChinese:
            writeInt(&buf, Int32(2))


        case .traditionalChinese:
            writeInt(&buf, Int32(3))


        case .czech:
            writeInt(&buf, Int32(4))


        case .french:
            writeInt(&buf, Int32(5))


        case .italian:
            writeInt(&buf, Int32(6))


        case .japanese:
            writeInt(&buf, Int32(7))


        case .korean:
            writeInt(&buf, Int32(8))


        case .portuguese:
            writeInt(&buf, Int32(9))


        case .spanish:
            writeInt(&buf, Int32(10))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeLanguage_lift(_ buf: RustBuffer) throws -> Language {
    return try FfiConverterTypeLanguage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeLanguage_lower(_ value: Language) -> RustBuffer {
    return FfiConverterTypeLanguage.lower(value)
}


// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum Network: Equatable, Hashable {

    case mainnet
    case testnet
    case regtest





}

#if compiler(>=6)
extension Network: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeNetwork: FfiConverterRustBuffer {
    typealias SwiftType = Network

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Network {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .mainnet

        case 2: return .testnet

        case 3: return .regtest

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: Network, into buf: inout [UInt8]) {
        switch value {


        case .mainnet:
            writeInt(&buf, Int32(1))


        case .testnet:
            writeInt(&buf, Int32(2))


        case .regtest:
            writeInt(&buf, Int32(3))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNetwork_lift(_ buf: RustBuffer) throws -> Network {
    return try FfiConverterTypeNetwork.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNetwork_lower(_ value: Network) -> RustBuffer {
    return FfiConverterTypeNetwork.lower(value)
}


// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum SigningInputKind: Equatable, Hashable {

    case p2pkh
    case p2shMultisig





}

#if compiler(>=6)
extension SigningInputKind: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSigningInputKind: FfiConverterRustBuffer {
    typealias SwiftType = SigningInputKind

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SigningInputKind {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .p2pkh

        case 2: return .p2shMultisig

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: SigningInputKind, into buf: inout [UInt8]) {
        switch value {


        case .p2pkh:
            writeInt(&buf, Int32(1))


        case .p2shMultisig:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningInputKind_lift(_ buf: RustBuffer) throws -> SigningInputKind {
    return try FfiConverterTypeSigningInputKind.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSigningInputKind_lower(_ value: SigningInputKind) -> RustBuffer {
    return FfiConverterTypeSigningInputKind.lower(value)
}


// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum TransactionOutputKind: Equatable, Hashable {

    case address
    case opReturn
    case expertRawScript





}

#if compiler(>=6)
extension TransactionOutputKind: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionOutputKind: FfiConverterRustBuffer {
    typealias SwiftType = TransactionOutputKind

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionOutputKind {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .address

        case 2: return .opReturn

        case 3: return .expertRawScript

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransactionOutputKind, into buf: inout [UInt8]) {
        switch value {


        case .address:
            writeInt(&buf, Int32(1))


        case .opReturn:
            writeInt(&buf, Int32(2))


        case .expertRawScript:
            writeInt(&buf, Int32(3))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOutputKind_lift(_ buf: RustBuffer) throws -> TransactionOutputKind {
    return try FfiConverterTypeTransactionOutputKind.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionOutputKind_lower(_ value: TransactionOutputKind) -> RustBuffer {
    return FfiConverterTypeTransactionOutputKind.lower(value)
}


// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum UtxoSignerKind: Equatable, Hashable {

    case wif
    case xprivDerivation





}

#if compiler(>=6)
extension UtxoSignerKind: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUtxoSignerKind: FfiConverterRustBuffer {
    typealias SwiftType = UtxoSignerKind

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UtxoSignerKind {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .wif

        case 2: return .xprivDerivation

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: UtxoSignerKind, into buf: inout [UInt8]) {
        switch value {


        case .wif:
            writeInt(&buf, Int32(1))


        case .xprivDerivation:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUtxoSignerKind_lift(_ buf: RustBuffer) throws -> UtxoSignerKind {
    return try FfiConverterTypeUtxoSignerKind.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUtxoSignerKind_lower(_ value: UtxoSignerKind) -> RustBuffer {
    return FfiConverterTypeUtxoSignerKind.lower(value)
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionUInt8: FfiConverterRustBuffer {
    typealias SwiftType = UInt8?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterUInt8.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterUInt8.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionUInt64: FfiConverterRustBuffer {
    typealias SwiftType = UInt64?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterUInt64.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterUInt64.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeChangeDestination: FfiConverterRustBuffer {
    typealias SwiftType = ChangeDestination?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeChangeDestination.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeChangeDestination.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSigningEnvelope: FfiConverterRustBuffer {
    typealias SwiftType = SigningEnvelope?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSigningEnvelope.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSigningEnvelope.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeXpriv: FfiConverterRustBuffer {
    typealias SwiftType = Xpriv?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeXpriv.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeXpriv.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceString: FfiConverterRustBuffer {
    typealias SwiftType = [String]

    public static func write(_ value: [String], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterString.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String] {
        let len: Int32 = try readInt(&buf)
        var seq = [String]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterString.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeAuditedInput: FfiConverterRustBuffer {
    typealias SwiftType = [AuditedInput]

    public static func write(_ value: [AuditedInput], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeAuditedInput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [AuditedInput] {
        let len: Int32 = try readInt(&buf)
        var seq = [AuditedInput]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeAuditedInput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSigningEnvelope: FfiConverterRustBuffer {
    typealias SwiftType = [SigningEnvelope]

    public static func write(_ value: [SigningEnvelope], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSigningEnvelope.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SigningEnvelope] {
        let len: Int32 = try readInt(&buf)
        var seq = [SigningEnvelope]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSigningEnvelope.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSigningEnvelopeInput: FfiConverterRustBuffer {
    typealias SwiftType = [SigningEnvelopeInput]

    public static func write(_ value: [SigningEnvelopeInput], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSigningEnvelopeInput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SigningEnvelopeInput] {
        let len: Int32 = try readInt(&buf)
        var seq = [SigningEnvelopeInput]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSigningEnvelopeInput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSigningEnvelopeSignature: FfiConverterRustBuffer {
    typealias SwiftType = [SigningEnvelopeSignature]

    public static func write(_ value: [SigningEnvelopeSignature], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSigningEnvelopeSignature.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SigningEnvelopeSignature] {
        let len: Int32 = try readInt(&buf)
        var seq = [SigningEnvelopeSignature]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSigningEnvelopeSignature.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSkippedInput: FfiConverterRustBuffer {
    typealias SwiftType = [SkippedInput]

    public static func write(_ value: [SkippedInput], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSkippedInput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SkippedInput] {
        let len: Int32 = try readInt(&buf)
        var seq = [SkippedInput]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSkippedInput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSpendableUtxo: FfiConverterRustBuffer {
    typealias SwiftType = [SpendableUtxo]

    public static func write(_ value: [SpendableUtxo], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSpendableUtxo.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SpendableUtxo] {
        let len: Int32 = try readInt(&buf)
        var seq = [SpendableUtxo]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSpendableUtxo.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTransactionOutput: FfiConverterRustBuffer {
    typealias SwiftType = [TransactionOutput]

    public static func write(_ value: [TransactionOutput], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTransactionOutput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TransactionOutput] {
        let len: Int32 = try readInt(&buf)
        var seq = [TransactionOutput]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTransactionOutput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeUtxoSigner: FfiConverterRustBuffer {
    typealias SwiftType = [UtxoSigner]

    public static func write(_ value: [UtxoSigner], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeUtxoSigner.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UtxoSigner] {
        let len: Int32 = try readInt(&buf)
        var seq = [UtxoSigner]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeUtxoSigner.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeXpub: FfiConverterRustBuffer {
    typealias SwiftType = [Xpub]

    public static func write(_ value: [Xpub], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeXpub.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Xpub] {
        let len: Int32 = try readInt(&buf)
        var seq = [Xpub]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeXpub.read(from: &buf))
        }
        return seq
    }
}
public func accountXprivFromMnemonic(phrase: String, passphrase: String?, language: Language, network: Network, account: UInt32)throws  -> AccountKeySet  {
    return try  FfiConverterTypeAccountKeySet_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_account_xpriv_from_mnemonic(
        FfiConverterString.lower(phrase),
        FfiConverterOptionString.lower(passphrase),
        FfiConverterTypeLanguage_lower(language),
        FfiConverterTypeNetwork_lower(network),
        FfiConverterUInt32.lower(account),$0
    )
})
}
public func addressFromWif(network: Network, wif: String)throws  -> WifInfo  {
    return try  FfiConverterTypeWifInfo_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_address_from_wif(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterString.lower(wif),$0
    )
})
}
public func combineSigningEnvelopes(envelopes: [SigningEnvelope])throws  -> SigningEnvelope  {
    return try  FfiConverterTypeSigningEnvelope_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_combine_signing_envelopes(
        FfiConverterSequenceTypeSigningEnvelope.lower(envelopes),$0
    )
})
}
public func composeAndSignTransaction(request: ComposeTransactionRequest)throws  -> ComposeTransactionResult  {
    return try  FfiConverterTypeComposeTransactionResult_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_compose_and_sign_transaction(
        FfiConverterTypeComposeTransactionRequest_lower(request),$0
    )
})
}
public func createMultisigDescriptor(network: Network, threshold: UInt8, cosignerXpubs: [Xpub], childPath: String, sorted: Bool)throws  -> MultisigDescriptor  {
    return try  FfiConverterTypeMultisigDescriptor_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_create_multisig_descriptor(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterUInt8.lower(threshold),
        FfiConverterSequenceTypeXpub.lower(cosignerXpubs),
        FfiConverterString.lower(childPath),
        FfiConverterBool.lower(sorted),$0
    )
})
}
public func deriveAddressFromXpriv(xpriv: Xpriv, path: String)throws  -> PathAddress  {
    return try  FfiConverterTypePathAddress_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_derive_address_from_xpriv(
        FfiConverterTypeXpriv_lower(xpriv),
        FfiConverterString.lower(path),$0
    )
})
}
public func deriveAddressFromXpub(xpub: Xpub, path: String)throws  -> PathAddress  {
    return try  FfiConverterTypePathAddress_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_derive_address_from_xpub(
        FfiConverterTypeXpub_lower(xpub),
        FfiConverterString.lower(path),$0
    )
})
}
public func derivePathFromXpriv(xpriv: Xpriv, path: String)throws  -> Xpriv  {
    return try  FfiConverterTypeXpriv_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_derive_path_from_xpriv(
        FfiConverterTypeXpriv_lower(xpriv),
        FfiConverterString.lower(path),$0
    )
})
}
public func derivePathFromXpub(xpub: Xpub, path: String)throws  -> Xpub  {
    return try  FfiConverterTypeXpub_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_derive_path_from_xpub(
        FfiConverterTypeXpub_lower(xpub),
        FfiConverterString.lower(path),$0
    )
})
}
public func finalizeSigningEnvelope(envelope: SigningEnvelope)throws  -> SignedTransaction  {
    return try  FfiConverterTypeSignedTransaction_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_finalize_signing_envelope(
        FfiConverterTypeSigningEnvelope_lower(envelope),$0
    )
})
}
public func generateMnemonic(options: MnemonicOptions)throws  -> GeneratedMnemonic  {
    return try  FfiConverterTypeGeneratedMnemonic_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_generate_mnemonic(
        FfiConverterTypeMnemonicOptions_lower(options),$0
    )
})
}
public func inspectXpriv(xpriv: Xpriv)throws  -> ExtendedKeyInfo  {
    return try  FfiConverterTypeExtendedKeyInfo_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_inspect_xpriv(
        FfiConverterTypeXpriv_lower(xpriv),$0
    )
})
}
public func inspectXpub(xpub: Xpub)throws  -> ExtendedKeyInfo  {
    return try  FfiConverterTypeExtendedKeyInfo_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_inspect_xpub(
        FfiConverterTypeXpub_lower(xpub),$0
    )
})
}
public func mnemonicToSeedHex(phrase: String, passphrase: String?, language: Language)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_mnemonic_to_seed_hex(
        FfiConverterString.lower(phrase),
        FfiConverterOptionString.lower(passphrase),
        FfiConverterTypeLanguage_lower(language),$0
    )
})
}
public func signMessage(network: Network, wif: String, message: String)throws  -> MessageSignature  {
    return try  FfiConverterTypeMessageSignature_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_sign_message(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterString.lower(wif),
        FfiConverterString.lower(message),$0
    )
})
}
public func signP2pkhTransaction(network: Network, unsignedTxHex: String, inputIndex: UInt64, scriptPubkeyHex: String, wif: String, sighashType: UInt32)throws  -> SignedTransaction  {
    return try  FfiConverterTypeSignedTransaction_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_sign_p2pkh_transaction(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterString.lower(unsignedTxHex),
        FfiConverterUInt64.lower(inputIndex),
        FfiConverterString.lower(scriptPubkeyHex),
        FfiConverterString.lower(wif),
        FfiConverterUInt32.lower(sighashType),$0
    )
})
}
public func signSigningEnvelope(envelope: SigningEnvelope, wif: String)throws  -> SigningEnvelope  {
    return try  FfiConverterTypeSigningEnvelope_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_sign_signing_envelope(
        FfiConverterTypeSigningEnvelope_lower(envelope),
        FfiConverterString.lower(wif),$0
    )
})
}
public func validateAddress(network: Network, address: String)throws  -> Bool  {
    return try  FfiConverterBool.lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_validate_address(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterString.lower(address),$0
    )
})
}
public func validateMnemonic(phrase: String, language: Language)throws  -> Bool  {
    return try  FfiConverterBool.lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_validate_mnemonic(
        FfiConverterString.lower(phrase),
        FfiConverterTypeLanguage_lower(language),$0
    )
})
}
public func verifyMessage(network: Network, address: String, signatureBase64: String, message: String)throws  -> Bool  {
    return try  FfiConverterBool.lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_verify_message(
        FfiConverterTypeNetwork_lower(network),
        FfiConverterString.lower(address),
        FfiConverterString.lower(signatureBase64),
        FfiConverterString.lower(message),$0
    )
})
}
public func wifFromXpriv(xpriv: Xpriv)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_wif_from_xpriv(
        FfiConverterTypeXpriv_lower(xpriv),$0
    )
})
}
public func xpubFromXpriv(xpriv: Xpriv)throws  -> Xpub  {
    return try  FfiConverterTypeXpub_lift(try rustCallWithError(FfiConverterTypeFfiError_lift) {
    uniffi_easydoge_km_ffi_fn_func_xpub_from_xpriv(
        FfiConverterTypeXpriv_lower(xpriv),$0
    )
})
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private let initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 30
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_easydoge_km_ffi_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_account_xpriv_from_mnemonic() != 4463) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_address_from_wif() != 58913) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_combine_signing_envelopes() != 26667) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_compose_and_sign_transaction() != 12869) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_create_multisig_descriptor() != 3691) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_derive_address_from_xpriv() != 65175) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_derive_address_from_xpub() != 58253) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_derive_path_from_xpriv() != 56170) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_derive_path_from_xpub() != 29172) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_finalize_signing_envelope() != 49330) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_generate_mnemonic() != 31845) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_inspect_xpriv() != 4835) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_inspect_xpub() != 11862) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_mnemonic_to_seed_hex() != 37706) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_sign_message() != 42106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_sign_p2pkh_transaction() != 60017) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_sign_signing_envelope() != 21938) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_validate_address() != 54103) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_validate_mnemonic() != 59801) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_verify_message() != 38433) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_wif_from_xpriv() != 4729) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_easydoge_km_ffi_checksum_func_xpub_from_xpriv() != 49159) {
        return InitializationResult.apiChecksumMismatch
    }

    return InitializationResult.ok
}()

// Make the ensure init function public so that other modules which have external type references to
// our types can call it.
public func uniffiEnsureEasydogeKmFfiInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all